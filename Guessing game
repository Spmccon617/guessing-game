from random import randint

while True:
	number = randint(1,100)
	guessNumber = input("What is the number?")

	if guessNumber is True:
			print("Good job!")
	else:
		print(number)
		print("Better luck next time")
		break
